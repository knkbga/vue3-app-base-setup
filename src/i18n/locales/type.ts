export type localeTypes = {
  navigation: {
    sideNavigation: {
      listItems: Array<{
        tasks: String
        courses: String
        participants: String
        messages: String
      }>
    }
  }
}