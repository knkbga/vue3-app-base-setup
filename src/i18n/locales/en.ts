import type { localeTypes } from './type'

const englishMessages: localeTypes = {
  navigation: {
    sideNavigation: {
      listItems: [
        {
          courses: 'Courses',
          messages: 'Messages',
          participants: 'Participants',
          tasks: 'Tasks'
        }
      ]
    }
  }
}

export default englishMessages
